server:
  port: 8003
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/level_up?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 1290
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    allow-circular-references: true
auth:
  url:
    auht-micro: http://localhost:8001/api/v1/authentication/token/verify
    url-verify: http://localhost:5173/
producto:
  url:
    get-id: http://localhost:8002/api/v1/productos/